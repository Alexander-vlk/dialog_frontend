export type User = {
    id: number,
    username: string,
    first_name: string,
    last_name: string,
    email: string,
}
export type UserProfile = {
    patronymicName: string,
    phoneNumber: string,
    imageUrl: string,
    gender: string,
    birthDate: string,
    diagnosisDate: string,
    diabetesType: string,
    treatmentType: string,
}
