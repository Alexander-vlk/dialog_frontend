export type Slide = {
    alt: string,
    image_url: string,
}

export type Advantage = {
    title: string,
    description: string,
    image_url: string,
    order_num: number,
}

export type Feature = {
    name: string,
    description: string,
    image_url: string,
}

export type Rate = {
    user_info: string,
    text: string,
}

export type FAQ = {
    question: string,
    answer: string,
}
